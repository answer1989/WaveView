<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.answer.waveview.MainActivity">

    <com.answer.waveview.lib.WaveView
        android:id="@+id/wave_view"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:fill_color="@color/colorPrimaryDark"
        app:shape="circle"
        app:stroke_color="@color/colorAccent"
        app:stroke_width="5dp"
        app:wave_duration="500"
        app:wave_height="10dp"
        app:wave_length="50dp" />

    <Button
        android:id="@+id/button_add_percentage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/wave_view"
        android:text="加10" />

    <Button
        android:id="@+id/button_anim"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="开始停止动画"
        android:layout_below="@+id/button_add_percentage"
        />
</RelativeLayout>
